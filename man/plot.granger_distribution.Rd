% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/grangersearch.R
\name{plot.granger_distribution}
\alias{plot.granger_distribution}
\title{Plot Granger Causality Distribution}
\usage{
\method{plot}{granger_distribution}(x, type = c("histogram", "density", "violin"), monochrome = FALSE, ...)
}
\arguments{
\item{x}{A \code{granger_distribution} object from \code{\link[=granger_distribution]{granger_distribution()}}.}

\item{type}{Character. Type of plot: "histogram" (default), "density", or "violin".}

\item{monochrome}{Logical. If TRUE, uses black/gray instead of colors.
Suitable for publications. Default FALSE.}

\item{...}{Additional arguments (ignored).}
}
\value{
Invisibly returns the input object.
}
\description{
Creates visualizations of the Granger causality strength distribution.
}
\details{
For multiple lags:
\itemize{
\item "histogram": Faceted histograms by lag
\item "density": Overlaid density curves colored by lag
\item "violin": Violin plots comparing distributions across lags
}
}
\examples{
set.seed(123)
df <- data.frame(
  A = cumsum(rnorm(100)),
  B = cumsum(rnorm(100)),
  C = cumsum(rnorm(100))
)
df$B <- c(0, 0.7 * df$A[1:99]) + rnorm(100, sd = 0.5)

dist <- granger_distribution(df, lag = 1:3)
plot(dist)
plot(dist, type = "density")
plot(dist, type = "violin")

}
