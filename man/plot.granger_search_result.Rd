% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/grangersearch.R
\name{plot.granger_search_result}
\alias{plot.granger_search_result}
\title{Plot Granger Causality Matrix}
\usage{
\method{plot}{granger_search_result}(
  x,
  type = c("pvalue", "significance", "statistic"),
  show_values = TRUE,
  gradient = TRUE,
  monochrome = FALSE,
  ...
)
}
\arguments{
\item{x}{A \code{granger_search_result} object from \code{\link[=granger_search]{granger_search()}}.}

\item{type}{Character. Type of values to display:
\itemize{
\item \code{"pvalue"} (default): Show -log10(p-value), with higher values indicating
stronger evidence of causality.
\item \code{"significance"}: Show binary significant/not significant.
\item \code{"statistic"}: Show the F-test statistic values.
}}

\item{show_values}{Logical. If TRUE, display numeric values in cells. Default is TRUE.}

\item{gradient}{Logical. If TRUE (default), use gradient coloring based on values.
If FALSE, cells are colored simply as significant (colored) or not significant (gray),
and actual p-values are displayed in cells when \code{type = "pvalue"}.}

\item{monochrome}{Logical. If TRUE, uses grayscale palette instead of blue.
Suitable for publications. Default FALSE.}

\item{...}{Additional arguments (ignored).}
}
\value{
Invisibly returns the input object.
}
\description{
Creates a heatmap-style matrix visualization of Granger causality relationships.
Displays two panels showing both directions of causality testing.
}
\details{
The visualization shows two panels:
\itemize{
\item Left panel: Tests whether row variable Granger-causes column variable
\item Right panel: Tests whether column variable Granger-causes row variable
}

For the \code{"pvalue"} type with \code{gradient = TRUE}, values are shown as -log10(p-value),
so larger values indicate stronger evidence of Granger causality. When \code{gradient = FALSE},
actual p-values are displayed in cells and coloring is binary (significant vs not).
}
\examples{
set.seed(42)
n <- 200
df <- data.frame(
  gdp = cumsum(rnorm(n)),
  consumption = cumsum(rnorm(n)),
  investment = cumsum(rnorm(n)),
  employment = cumsum(rnorm(n))
)
# Add some causal structure
df$consumption <- c(0, 0.5 * df$gdp[1:(n-1)]) + rnorm(n, sd = 0.5)
df$employment <- c(0, 0.3 * df$gdp[1:(n-1)]) + rnorm(n, sd = 0.5)

# Run exhaustive search (include all results for complete matrix)
results <- granger_search(df, include_insignificant = TRUE)

# Plot as matrix with gradient
plot(results)

# Plot without gradient, showing p-values
plot(results, gradient = FALSE)

# Show binary significance
plot(results, type = "significance")

}
\seealso{
\code{\link[=granger_search]{granger_search()}} for running the exhaustive search.
}
